{
  "name": "plushie-panic-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
